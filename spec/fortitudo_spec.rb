require 'spec_helper'

describe Fortitudo do
  it 'has a version number' do
    expect(Fortitudo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
